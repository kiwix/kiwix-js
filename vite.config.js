import { defineConfig } from 'vite'

export default defineConfig({
    server: {
        open: 'www/index.html'
    }
    // build: {
    //     rollupOptions: {
    //         input: 'www/index.html',
    //     }
    // }
});
